rules_version = '2';

// 이 Firestore는 Cloud Functions가 관제사 접속 상태를 기억하는 용도로만 씁니다.
// 클라이언트는 읽을 일도 쓸 일도 없으므로 전부 차단합니다.
// Admin SDK(함수 쪽)는 이 규칙의 적용을 받지 않습니다.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
